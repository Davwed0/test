# Database Environment Variables
# MegaBank Production Database Configuration

# PostgreSQL Connection
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=banking_db
export DB_USER=bank_user
# BUG INTENTIONAL: Wrong password (missing exclamation mark)
export DB_PASSWORD=BankSecure2024

# Application Settings
export APP_PORT=8080
export ENVIRONMENT=production

# BUG INTENTIONAL: Flask debug mode should NEVER be on in production!
export FLASK_DEBUG=1
export FLASK_ENV=production
